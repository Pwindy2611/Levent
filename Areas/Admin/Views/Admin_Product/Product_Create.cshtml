@model Levent.Models.Product

@{
    ViewBag.Title = "Product_Create";
    Layout = "~/Areas/Admin/Views/Shared/LayoutAdmin.cshtml";
}

<h2>Product_Create</h2>

@using (Html.BeginForm("Product_Create", "Admin_Product", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
    <div class="form-horizontal">
        <h4>Room</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="card">
            <div class="card-body">
                <p class="card-description">Create </p>
                <form class="forms-sample">
                    <div class="form-group">
                        <label for="exampleInputName1">ID</label>
                        @Html.EditorFor(model => model.ID_Pro, new { htmlAttributes = new { @class = "form-control", @readonly = "readonly" } })
                        @Html.ValidationMessageFor(model => model.ID_Pro)
                    </div>
                    <div class="form-group">
                        <label for="exampleInputEmail3">Name</label>
                        @Html.EditorFor(model => model.Name_Pro, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => model.Name_Pro, "", new { @id = "exampleInputEmail3" })
                    </div>
                    <div class="form-group">
                        <label for="exampleInputPassword4">Price</label>
                        @Html.EditorFor(model => model.Price_pro, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => model.Price_pro, "", new { @id = "exampleInputName1" })
                    </div>
                    <div class="form-group">
                        <label style="display:block" for="exampleSelectGender">SelectCate</label>
                        @{Html.RenderAction("SelectCate", "Admin_Product");}
                    </div>
                    <div class="form-group">
                        <label>File upload</label>
                        <input type="file" name="img[]" class="file-upload-default">
                        <div class="input-group col-xs-12">
                            <input type="text" class="form-control file-upload-info" disabled="" placeholder="Upload Image">
                            <span class="input-group-append">
                                <label for="uploadimg" class="file-upload-browse btn btn-gradient-primary" type="button">Upload</label>
                                <input id="uploadimg" style="display:none" type="file" name="UploadImage" onchange="ChangeImage(this,document.getElementById('previewImg'))" />
                            </span>
                        </div>
                        <img src="@Url.Content(Model.Image_Pro)" height="150" width="150" id="previewImg" />
                    </div>
                    <button type="submit" class="btn btn-gradient-primary me-2">Submit</button>
                    <button class="btn btn-light">Cancel</button>
                </form>
            </div>
        </div>
    </div>
}
